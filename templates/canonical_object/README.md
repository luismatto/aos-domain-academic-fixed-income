# Canonical Object Template

Each Canonical Academic Object must include:

```text
metadata.yaml
knowledge.yaml
evidence.yaml
professional.yaml
learning.yaml
manifest.yaml
```

The domain scripts discover objects automatically from `objects/CAO###`.
