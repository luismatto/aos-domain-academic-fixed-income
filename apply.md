# Apply MR-001

1. Unzip this package.
2. Copy the contents of `overlay/` into the root of `aos-domain-academic-fixed-income`.
3. Replace existing files when prompted.
4. Run:

```r
source("scripts/run_tests.R")
```
