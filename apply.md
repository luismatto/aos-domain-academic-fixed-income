# Apply FI-DER-0005 Validation Candidate

1. Unzip this package.
2. Copy the contents of `overlay/` into the repository root.
3. Replace existing files when prompted.
4. Run in RStudio:

```r
source("scripts/run_tests.R")
```
